# Orchestra source manifest — generated by 'orchestra generate-manifest'
# Fields per package: name, type, path
# Types: agent | prompt | prompt-dir | skill

packages:
  # --- Agents ---
  - name: demo-agent
    type: agent
    path: agents/demo-agent.agent.md
  - name: second-agent
    type: agent
    path: agents/second-agent.agent.md

  # --- Prompts ---
  - name: demo-prompt
    type: prompt
    path: prompts/demo-prompt.prompt.md

  # --- Prompt snippets (multi-file package) ---
  - name: snippets
    type: prompt-dir
    path: prompts/snippets/

  # --- Skills ---
  - name: demo-skill
    type: skill
    path: skills/demo-skill/

